//https://sought-fly-48.clerk.accounts.dev

const authConfig = {
  providers: [
    {
      domain: "https://sought-fly-48.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};

export default authConfig;
